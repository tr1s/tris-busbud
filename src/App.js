function App() {
  return (
    <div className="App" style={{ margin: "20px" }}>
      <p>
        Run `<strong>npm run storybook</strong>` to have a look at all the
        available components!
      </p>
    </div>
  );
}

export default App;
